/**
 * Single source of truth for trust metrics across the application
 * Update these values to maintain consistency across all pages
 */
export const TRUST_METRICS = {
  companies: 115,
  visitorsIdentified: 2300000,
  averageConversionIncrease: 340,
  yearlyRevenueCaptured: 4800000,
} as const;
